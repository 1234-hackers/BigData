to run the app
Required:
Python 3.11.3 or later.
pip 23.2.1
run commands

#install libs used
pip install -r re.txt

#once complete,

streamlit run app.py

Open the url provided in the  browser

NOTE:
The application assumes that MongoDb service is running in the localhost via port 27017
